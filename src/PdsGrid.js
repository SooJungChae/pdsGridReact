import React, { Component } from 'react';
import Grid from './components/Grid';

class PdsGrid extends Component {
    render() {
        return (
            <Grid></Grid>
        );
    }
}

export default PdsGrid;
